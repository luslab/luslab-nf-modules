# This file creates a conda environment for the guppy module
#   conda env create -f environment.yml
name: nfcore-module-guppy
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.7
  - pycoqc=2.5.0.21