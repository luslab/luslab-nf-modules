# This file creates a conda environment for the get_crosslinks module
#   conda env create -f environment.yml
name: nfcore-module-getcrosslinks
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - pigz=2.3.4
  - bedtools=2.28.0